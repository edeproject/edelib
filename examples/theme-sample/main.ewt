;; vim:ft=scheme:expandtab
;; This is sample file for ede widget theme (ewt).

(theme.name   "Sample Theme")
(theme.author "John Foo <john@foo.com>")
(theme.sample "img.jpg")

(define (get-sample)
 "this is an sample"
)

(theme.style "ede" 
[
  (scheme             "gtk+")
  (foreground_color   "#fff")
  (font_normal        "-schumacher-*-medium-*-*-*-12-*-*-*-*-*-*-*")
  (icon_theme         "edeneu")
  (sample             (get-sample))
])

(include "xxx.ewt")
